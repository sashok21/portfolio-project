# Portfolio Website - Django CMS

Професійний портфоліо сайт на Django з CMS системою для управління контентом.

## 🚀 Встановлення

### 1. Структура статичних файлів

Створіть наступну структуру папок в вашому проекті:

```
portfolio/
├── static/
│   └── portfolio/
│       ├── css/
│       │   └── style.css
│       └── js/
│           └── main.js
└── templates/
    └── portfolio/
        └── home.html
```

### 2. Копіювання файлів

Скопіюйте файли на свої місця:

**HTML Template:**
```bash
cp home_template.html portfolio/templates/portfolio/home.html
```

**CSS:**
```bash
cp style.css portfolio/static/portfolio/css/style.css
```

**JavaScript:**
```bash
cp main.js portfolio/static/portfolio/js/main.js
```

**Views:**
```bash
cp views.py portfolio/views.py
```

### 3. Налаштування settings.py

Переконайтеся, що в `core/settings.py` правильно налаштовані статичні файли:

```python
STATIC_URL = 'static/'
STATICFILES_DIRS = []

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# Для продакшену додайте:
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
```

### 4. Збір статичних файлів (для продакшену)

```bash
python manage.py collectstatic
```

### 5. Міграції бази даних

```bash
python manage.py makemigrations
python manage.py migrate
```

### 6. Створення суперюзера

```bash
python manage.py createsuperuser
```

### 7. Запуск сервера

```bash
python manage.py runserver
```

Відкрийте браузер: http://localhost:8000

## 📊 Адмін панель

Адмін панель доступна за адресою: http://localhost:8000/admin/

### Додавання контенту:

1. **Профіль власника** - основна інформація про вас
   - Ім'я та прізвище
   - Посада
   - Фото профілю
   - Текст "Про себе"
   - Контактні дані (email, телефон, соц. мережі)

2. **Досвід роботи**
   - Компанія
   - Посада
   - Період
   - Опис
   - Порядок відображення

3. **Послуги**
   - Назва послуги
   - Картинка
   - Посилання (опціонально)

4. **Навички (Skills)**
   - Назва
   - Іконка

## 🎨 Особливості дизайну

- **Шрифт:** Montserrat (Google Fonts)
- **Колір акценту:** #00FFEA (бірюзовий)
- **Адаптивний дизайн** для всіх пристроїв
- **Smooth scroll** навігація
- **Анімації** при скролі
- **Hover ефекти** на картках та елементах

## 📱 Responsive

- Desktop: 1200px+
- Tablet: 768px - 1199px
- Mobile: до 767px

## 🔧 Технології

- Django 6.0
- HTML5
- CSS3
- JavaScript (Vanilla)
- Google Fonts
- Unfold Admin

## 📝 Додаткові можливості

### JavaScript функції:

- Активна навігація при скролі
- Анімація timeline елементів
- Parallax ефект для фото
- Lazy loading зображень
- Accessibility покращення
- Performance моніторинг

### CSS features:

- CSS Custom Properties (змінні)
- Flexbox & Grid Layout
- Smooth animations
- Custom scrollbar
- Media queries

## 🎯 Оптимізація

- Мінімізація CSS/JS (для продакшену)
- Lazy loading зображень
- Оптимізовані шрифти
- Кешування статики

## 📦 Структура проекту

```
portfolio-project/
├── core/
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── portfolio/
│   ├── models.py
│   ├── views.py
│   ├── admin.py
│   ├── static/
│   │   └── portfolio/
│   │       ├── css/
│   │       │   └── style.css
│   │       └── js/
│   │           └── main.js
│   └── templates/
│       └── portfolio/
│           └── home.html
├── media/
│   ├── profile/
│   ├── services/
│   └── skills/
└── manage.py
```

## 🐛 Troubleshooting

### Статичні файли не завантажуються:

```bash
python manage.py collectstatic --clear
```

### Помилка з медіа файлами:

Перевірте `MEDIA_ROOT` та `MEDIA_URL` в settings.py

### База даних:

```bash
python manage.py migrate --run-syncdb
```

## 📞 Підтримка

Для питань та підтримки звертайтесь до розробника.

## 📄 Ліцензія

Цей проект створений для персонального використання.